<div>
  <%= simple_form_for([artwork, booking]) do |f| %>
    <%= f.input :start_date, label: 'Date de début' %>
    <%= f.input :end_date, label: 'Date de fin' %>
    <%= f.submit 'Réserver maintenant', class: "btn btn-flat-big mt-4", style: "width: 200px" %>
  <% end %>
</div>
